<header>
<h1>Les établissements de l'ADSEAAV</h1>
</header>
  <div class="row">
    <div class="col-12 col-md-8">
      <div
        id="map"
        style="width: 100%;
        height: 600px;
        position: sticky;
        top: 8px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
      ></div>
    </div>
    <div class="col-12 col-md-4">
    <% if policy(@sites).new? %>
    <div class="bg-warning arondi m-3 p-3 text-white">
      <%= link_to new_site_path do %>
      <i class="fas fa-pencil-alt"></i>Ajouter
      <% end %>
    </div>
    <% end %>
      <%= render 'card_site' %>
    </div>
  </div>
